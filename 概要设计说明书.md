
# 发电负荷预测平台 - 概要设计说明

## 一、系统架构
采用分层设计：
1. 数据采集层：导入CSV文件，解析数据
2. 数据存储层：PostgreSQL（业务数据）+ InfluxDB（时序数据）
3. 数据处理层：数据清洗、特征工程、模型训练
4. 业务逻辑层：预测、分析、用户管理
5. 展示层：Dashboard可视化

## 二、模块设计

### 1. 数据采集模块
- DataImporter：导入CSV文件
- FileParser：解析文件内容
- 数据校验和格式转换

### 2. 预测模块
- LoadPredictor：发电负荷预测
- DemandPredictor：供电需求预测
- AGCPredictor：AGC信号感知

### 3. 分析模块
- CorrelationAnalyzer：相关性分析
- 热力图、散点图展示

### 4. 管理模块
- UserService：用户管理
- AuthService：权限控制
- DashboardController：页面展示

## 三、数据库表

### 1. 用户表 (users)
id, username, password, role, email, created_at

### 2. 采集数据表 (collection_data)
id, tag_name, server_ip, value, timestamp, data_type

### 3. 气象数据表 (weather_data)
id, station_id, temperature, humidity, wind_speed, forecast_date

### 4. 预测结果表 (predictions)
id, prediction_type, forecast_time, predicted_value, confidence

## 四、API接口

| 接口 | 方法 | 功能 |
|------|------|------|
| /api/auth/login | POST | 用户登录 |
| /api/data/collection | POST | 导入采集数据 |
| /api/data/weather | POST | 导入气象数据 |
| /api/predict/load | POST | 负荷预测 |
| /api/predict/demand | POST | 需求预测 |
| /api/predict/agc | POST | AGC预测 |
| /api/analysis/correlation | POST | 相关性分析 |

## 五、技术栈
- 前端：Vue.js + ECharts
- 后端：FastAPI (Python)
- 数据库：PostgreSQL + InfluxDB
- 机器学习：Scikit-learn / TensorFlow
- 部署：Docker

## 六、目录结构
```
backend/
├── api/          # 接口
│   ├── auth.py
│   ├── data.py
│   ├── predict.py
│   └── analysis.py
├── models/       # 数据库模型
├── services/     # 业务服务
├── ml/           # 机器学习
└── main.py

frontend/
├── src/
│   ├── components/
│   ├── views/
│   └── api/
└── package.json
```
